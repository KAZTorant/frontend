# Backend API Configuration
# Specify the IP address and port of your backend API server
VUE_APP_API_HOST=localhost
VUE_APP_API_PORT=8000

# Optional: API protocol (http or https)
VUE_APP_API_PROTOCOL=http

# Frontend Configuration
# Specify the IP address and port for the frontend dev server
VUE_APP_FRONTEND_HOST=localhost
VUE_APP_FRONTEND_PORT=8080
